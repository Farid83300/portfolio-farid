# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

Personal portfolio site for Farid Zaffalone (freelance PHP/React & WordPress developer, Draguignan, France). Monorepo with two independent apps:

- `frontend/` — Next.js 15 (App Router) site, deployed standalone. Also hosts the `/admin` dashboard (JWT + 2FA protected) under `app/admin/`.
- `backend/` — hand-rolled PHP 8.1+ MVC API (no framework). Consumed by the frontend for the public contact form, for the public blog/portfolio/services pages (real posts/projects/services, see below), and by the `/admin` dashboard for auth/2FA and CRUD on posts, projects, services, categories, tags, comments, contact messages, chat messages, newsletter subscribers, and image uploads.

Commit messages and UI copy are in French.

Do not scan `frontend/node_modules`, `frontend/.next`, or `backend/vendor` — they are dependency/build output and irrelevant to the code.

## Frontend (`frontend/`)

### Commands

Package manager is **yarn** (`yarn.lock` is the source of truth — do not use `npm install`/`npm run` or generate a `package-lock.json`).

```bash
yarn dev      # dev server, http://localhost:3000
yarn build    # production build
yarn start    # serve the production build
yarn lint     # eslint
yarn format   # prettier --write .
```

No test suite exists in this project.

Requires `frontend/.env.local` (see `.env.local.example`) with `NEXT_PUBLIC_API_URL` pointing at the backend. The user runs the backend via **MAMP** (Apache on `:8888`, `backend/` symlinked into MAMP's `htdocs` as `portfolio-farid-backend`), so the working value is `http://localhost:8888/portfolio-farid-backend/public` — the PHP built-in server (`http://localhost:8000`) also works as a fallback (`php -S localhost:8000 -t public` from `backend/`) but isn't what's normally running locally. Only one of these should be up at a time; if the Next dev server can't reach the API, check `lsof -i :8888`/`:8000` before assuming the code is broken.

### Origin and current state

Built from the "Reeni" Next.js portfolio template (ThemeForest/Inversweb), which originally shipped 22 home-page demos, each in dark and "-white" (light) variants, plus vendor demo-switcher UI. **Only demo 1, dark mode, is kept.** All other demos, all `-white` route variants, and every trace of the original template's branding (the "Reeni"/"Inversweb"/ThemeForest name, demo-switcher modal, "buy this template" button, demo screenshots) have been removed as of the 2026-09-02 cleanup. Concretely this means:

- Home page is `app/page.js` at the app root — there is no `/index-01` route.
- Surviving routes: `/`, `/about`, `/contact`, `/blog` (+ `/blog/category/[category]`, `/blog/tag/[tag]`, `/blog-details/[slug]`), `/project` (+ `/project-details/[slug]`), `/service` (+ `/service-details/[slug]`), `/mentions-legales`, `/politique-de-confidentialite`.
- Surviving header/footer/nav components: `Header1`, `Nav1`, `Sidebar`, `MobileMenu`, `Footer1`, `Footer3`, `Copyright`. `Header2-5`, `Footer2/4/5`, the "onepage" nav variants, and the demo-switcher components (`DemoToggler`, `Demomodal`) were deleted as dead code — do not re-add them.
- The logo lives at `public/assets/images/logo/logo.png` (already the user's real logo, referenced as both `darkLogo` and `lightLogo` defaults).
- Some components (e.g. `components/blog/Blogs.jsx`, `components/common/Portfolio*.jsx`) still carry an unused `isLight` prop that toggled between dark/white route variants. It's harmless dead code (the branch is now unreachable) — no need to touch it unless doing a deeper refactor. `components/services/Services.jsx` and `components/services/ServiceDetails.jsx` had this prop removed entirely when they were migrated to API-backed data (see below) — don't re-add it there.
- Real content lives in `frontend/data/*.js` (`blogs.js`, `portfolio.js`, `services.js`, `menu.js`, etc.); several entries still have template placeholder copy (Latin filler text, generic testimonials) that hasn't been rewritten yet. `services.js`'s `services`/`services4`/`allServices` exports were removed during the services API migration (2026-09-05) since nothing imports them anymore — the remaining exports (`services2`, `services3`, `serviceCards*`, `services5`) are still static demo data for the *other*, non-migrated `Services2-6` section variants (see below).

### Architecture

- App Router pages under `app/`, grouped by route type: `(homes)` no longer used (only the root `page.js` remains), `(blogs)`, `(projects)`, `(services)`, plus `about/` and `contact/`.
- Each page composes section components from `components/common/` (many numbered variants like `Services`, `Services2`, `Services3` — these are legitimate alternate section layouts mixed into the single surviving demo, not per-demo duplicates; don't assume unused ones are dead).
- `components/homes/home-1/` holds the hero/testimonials pieces specific to the one surviving home page.
- `components/common/CommonComponents.jsx` is mounted on every page and renders global floating UI: `Chat`, `ScrollTop`, `Sidebar`, `MobileMenu`.
- Styling is SCSS under `public/assets/scss/`, imported once in `app/layout.js` via `main.scss`. Bootstrap 5 + custom template SCSS. All SCSS/CSS lives under `public/assets/scss/` (or `public/assets/css/`) — including scoped CSS Modules like the admin dashboard's, never colocated next to the `.jsx` files that use them.
- **Blog, portfolio and services content is real, API-backed data** — `/blog`, `/blog-details/[slug]`, `/blog/category/[category]`, `/blog/tag/[tag]`, `/project`, `/project-details/[slug]`, `/service`, and `/service-details/[slug]` all fetch from the backend via `lib/publicApi.js` (`getPosts`, `getPost`, `getProjects`, `getProject`, `getServices`, `getService`, all server-side `fetch` with `cache: 'no-store'`) instead of importing `frontend/data/blogs.js`/`portfolio.js`/`services.js`. `uploadUrl(path)` in the same file builds the full image URL from `NEXT_PUBLIC_API_URL` + the relative path the API returns (e.g. `articles/xxx.webp`) — those origins must be listed in `next.config.mjs`'s `images.remotePatterns` for `next/image` to accept them.
  - Nav menu (`menu.js`) is still static.
  - **Services migration (2026-09-05)**: `components/services/Services.jsx` (the `/service` listing) and `components/services/ServiceDetails.jsx` (detail page) are async server components calling `getServices()`/`getService(slug)` instead of importing `data/services.js`'s `services4`/`allServices`. The home page's and about page's services teaser (`components/common/Services.jsx`, mounted by `app/page.js` and `app/about/page.jsx`) was migrated too — it now shows the first 4 API services (by `sort_order`) instead of a separate hardcoded 4-item list, so there's a single source of truth for service content across the whole site. `app/(services)/service-details/[slug]/page.jsx` calls `notFound()` (from `next/navigation`) when `getService(slug)` returns null, same convention as `blog-details`/`project-details`. A service's `sections` field (JSON array of `{heading, paragraphs: string[]}`, editable from the admin) renders as repeated `<h2>`/`<p>` blocks on the detail page — this is what replaced the old hardcoded Lorem-ipsum-style body copy that used to be identical on every service page.
  - The home page's "recent blog" teaser (`components/common/Blogs.jsx`, mounted by `app/page.js`) and "latest portfolio" teaser (`components/common/Portfolio2.jsx`, also mounted by `app/page.js`) were both migrated the same way (2026-09-05/06): async server components calling `getPosts()`/`getProjects()` and rendering the first 3/4 items (real image via `uploadUrl`, real title/slug/subtitle, real `published_at` for posts), not demo-data static lists anymore. `frontend/data/blogs.js`/`portfolio.js` still exist and are still imported by the *other* unrelated demo components (`components/common/Blogs2-4.jsx`, `components/common/Portfolio.jsx`/`Portfolio3.jsx`) — those were intentionally left alone, don't assume they're dead or that they should also be wired to the API without being asked.
  - `components/blog/Blogs.jsx` (the `/blog` listing) vs `components/common/Blogs.jsx` (the home page teaser) vs `components/common/Blogs2-4.jsx` (other demo sections, still static) are separate files with similar names — same split exists for `components/projects/Projects.jsx` (the `/project` listing) vs `components/common/Portfolio2.jsx` (the home page teaser, now also real/API-backed but a different component with different markup/props) vs `components/common/Portfolio.jsx`/`Portfolio3.jsx` (other demo sections, still static demo data). Check which one a page actually imports before editing.
  - Card-list thumbnails (`Blogs.jsx`, `Projects.jsx`, `BlogSidebar.jsx`'s recent-post thumb) sit inside template CSS boxes with a fixed height + `object-fit: cover`, so they crop safely regardless of the uploaded image's ratio. The two full-width "hero" images (`BlogDetails.jsx`'s `.thumbnail-top`, `ProjectDetails.jsx`'s `.project-details-thumnail-wrap`) and the project gallery swiper images had **no** such CSS — an image uploaded in the wrong ratio would visibly stretch/distort. All three now carry an inline `style={{ aspectRatio, objectFit: 'cover' }}` matching the `width`/`height` props on the same `<Image>`, so any ratio crops instead of stretching. Recommended upload sizes (paysage/landscape, JPG or WebP, ≤4 MB): featured image ~1200×650, article cover ~1600×830, project thumbnail ~1200×990, project cover ~1600×700, gallery images ~820×590.
  - `BlogDetails.jsx` and `ProjectDetails.jsx` render `blog.content`/`portfolioItem.description` (rich-text HTML from the admin editor) via `dangerouslySetInnerHTML`, piped through `isomorphic-dompurify`'s `DOMPurify.sanitize()` first — keep that wrapper if you touch either component, it's what stops a compromised/careless admin session from turning stored content into a stored-XSS payload against site visitors.
  - `ProjectDetails.jsx` (2026-09-06): next to the title, an "Aperçu du site" `<a>` (styled via `.project-preview-btn` in `public/assets/scss/elements/_project-preview.scss`, site-primary-colored pill button) renders only when `portfolioItem.live_url` is set, opening it in a new tab (`target="_blank"`) — plain outbound link, no modal/iframe (a modal+iframe preview was tried first and explicitly reverted: many sites send `frame-ancestors` CSP headers that block being framed at all, making an in-page preview unreliable, so a new tab is the only approach guaranteed to work for every project). The pre-existing "Voir le site" link in the sidebar (`portfolioItem.live_url` again) still does the same thing — both exist, don't consolidate them into one without being asked.
- New page/component files under `app/` and `components/` are `.jsx` (not `.js`) unless they're plain non-JSX modules (e.g. `app/layout.js`/`app/page.js` predate this convention and were left as-is; `lib/adminApi.js` has no JSX so it stays `.js`).
- `app/not-found.jsx` — custom 404 page (Next.js App Router convention, catches every unmatched route and every explicit `notFound()` call, e.g. from `blog-details`/`project-details`/`service-details`). Reuses `Header1`/`Footer3`/`Copyright`/`CommonComponents` like every other page (there's no shared header/footer in `app/layout.js`) and the site's existing `tmp-btn hover-icon-reverse` CTA button markup. The wrapping `breadcrumb-area` section is forced to `min-height: 100vh` (inline style) so the footer sits below the fold instead of being immediately visible on load. Its "Retour à l'accueil" button wrapper carries the same `tmp-scroll-trigger tmp-fade-in animation-order-4` classes as the home Hero's "Prendre RDV" button (2026-09-06 fix — it was missing them, so the button popped in at full opacity instantly instead of playing the same fade-in-up transition, which read as a flicker/blink on load); if you touch this button again, keep it byte-for-byte matching `components/homes/home-1/Hero.jsx`'s button markup.
- **Legal pages (2026-09-05)**: `app/mentions-legales/page.jsx` and `app/politique-de-confidentialite/page.jsx` are static French legal pages (LCEN mentions légales + RGPD privacy policy), linked from `components/footers/Copyright.jsx`. Same page shell as every other page (`Header1`/breadcrumb-area/`Footer3`/`Copyright`/`CommonComponents`), content wrapped in `.blog-classic-area-wrapper` + `.disc` (reusing `BlogDetails.jsx`'s content styling) with plain `<h2>`/`<p>`/`<ul>` relying on the site's global typography — no new content SCSS needed. Site identity used there: micro-entrepreneur, SIRET 499 907 285 00047, TVA non applicable (art. 293 B CGI), domain `faridzaffalone.com`, hosting split frontend/Vercel + backend-DB/o2switch. Update these two pages by hand if the legal status, hosting, or data collection (contact form/chat/newsletter/analytics) changes. The privacy policy's section 3 also covers the Calendly link (see below) — update it too if that integration ever changes (e.g. if it becomes an embedded widget instead of a plain outbound link, which would actually set third-party cookies on this site and need its own consent handling like GA/Clarity).
- **Booking link (2026-09-06)**: the home Hero's "Prendre RDV" button (`components/homes/home-1/Hero.jsx`) is a plain external `<a target="_blank" rel="noopener noreferrer">` to `https://calendly.com/farid-zaffalone/30min`, not a `next/link` to `/contact` anymore. No data is passed to Calendly by this site (no prefilled query params), so the privacy policy just documents it as an outbound link to a third party the visitor may choose to book through — see section 3.
- **Cookie consent (2026-09-05)**: `app/layout.js` used to load Google Analytics (`G-J7VRJ4H2H5`) and Microsoft Clarity (`uetk0l9p9w`) unconditionally in `<head>` — that was a RGPD/CNIL compliance gap (analytics cookies require consent). Both `<Script>` blocks were removed from `layout.js` and moved into `components/common/CookieConsent.jsx` (mounted globally in `app/layout.js`'s `<body>`, alongside `LayoutWrapper`), which only renders those `<Script>` tags once the visitor has accepted. Consent choice (`'accepted'`/`'refused'`) is stored in `localStorage` under `ppf-cookie-consent`; the banner (styled in `public/assets/scss/elements/_cookie-consent.scss`, using the site's `--color-primary`/`--background-color-3` theme variables) shows once until a choice is made and has a single "Accepter" button plus a small × close icon at the banner's right edge that dismisses it and records a refusal — there is intentionally no "Refuser" button/label. If GA/Clarity IDs ever change, update the constants at the top of `CookieConsent.jsx` (not `layout.js`).

### Admin dashboard (`app/admin/`)

Client-side React admin UI, isolated from the public site (no shared header/footer/global chrome — each `/admin/*` page composes only `AdminSidebar` via `app/admin/layout.jsx`).

- `app/admin/layout.jsx` — auth guard (redirects to `/admin/login` if no JWT in `localStorage`; redirects to `/admin/security` if the token's `scope` is `setup_2fa`, forcing 2FA setup before anything else is reachable).
- `app/admin/login/page.jsx` — email/password + conditional TOTP code field (2-step, same form).
- `app/admin/security/page.jsx` — 2FA enrollment (QR code via the `qrcode` npm package, rendered client-side from the backend's `otpauth://` URI) and disable flow.
- `app/admin/{messages,chat,newsletter,comments}/page.jsx` — list/moderate views over contact messages, chat widget messages, newsletter subscribers, blog comments.
- `app/admin/{articles,projects,services}/` — CRUD (list + `new/` + `[id]/edit/`) over posts/projects/services, using shared `components/admin/{PostForm,ProjectForm,ServiceForm}.jsx`. `PostForm` has status/publish date/category select (with inline "create category")/tag checkboxes (with inline "create tag")/featured image upload+alt/SEO fields; `ProjectForm` has subtitle/category (free text)/client/role/date/CSV tags/features (one per line)/live URL/sort order/thumbnail upload/cover image upload/multi-image gallery upload/SEO fields; `ServiceForm` has status (draft/published)/icon (Font Awesome class)/description/sort order/single image upload+alt/SEO fields, plus a repeatable "sections" editor (add/remove blocks of heading + one-paragraph-per-line textarea) that maps to the `sections` JSON column consumed by the public detail page.
- `lib/adminApi.js` — fetch wrapper: reads `NEXT_PUBLIC_API_URL`, attaches the `localStorage` JWT, redirects to login on 401. Also exports `adminUploadFile(file, dir)` for image uploads — sends `multipart/form-data` (no `Content-Type` header, left to the browser for the boundary) to `POST /admin/uploads`, `dir` is one of `articles`, `projects`, `projects/gallery`, `services`.
- Styling: `public/assets/scss/admin/admin.module.scss`, a CSS Module imported via the `@/public/assets/scss/admin/admin.module.scss` alias — deliberately not merged into `main.scss` so the public site's global styles are untouched.
- The floating "Message Direct" chat widget (`components/common/Chat.jsx`, 2026-09-05) is wired to the backend: `POST /chat-messages` (public, validates name/email/message) feeds the same `chat_messages` table the admin already moderates. On success it shows a `react-toastify` success toast and resets the form; on failure (validation or network) it shows an error toast and keeps the entered values. Same pattern as the newsletter signup form (also wired, see below) and the blog comment form (`components/blog/Comment.jsx`, 2026-09-06, `POST /comments`) — same toast/reset behavior, plus its submit control was fixed from an `<a href="#">` to a real `<button type="submit">` (the `href="#"` was causing the page to jump back to the top on every submit, since that's the browser's default behavior for an empty-hash anchor — same footgun family as the newsletter button below, an icon/CTA-looking element inside a `<form>` that isn't actually a submit control).
- **Newsletter badge (2026-09-06)**: the sidebar's "Newsletter" count used to be the *total* active-subscriber count (`newsletter_count`, still shown on the dashboard stat tile), so it never went away. It's now driven by a separate `newsletter_new_count` (subscribers with `viewed_at IS NULL`, see `migrations_v6.sql`) that clears automatically: `app/admin/layout.jsx`'s per-navigation effect calls `PUT /admin/newsletter/mark-viewed` (only when `pathname === '/admin/newsletter'`) *before* fetching `/admin/dashboard`, so by the time the sidebar badge re-renders after navigating into that page, the count is already 0 — no per-row "mark read" click needed, unlike Messages/Chat.
- **Footgun (fixed 2026-09-05)**: `components/footers/NewsletterForm.jsx`'s submit logic (fetch + toast + reset) was correct from the start, but the visible envelope icon was a decorative `<span className="form-icon">` instead of a `<button type="submit">` — with no submit button anywhere in the form, neither clicking the icon nor pressing Enter could ever submit it (a lone `<span>` has no default action, and HTML's implicit Enter-to-submit requires at least one real submit button to exist). `_footer.scss`'s `.newsletter-form-1 button` rule was already there, unused, apparently written for a `<button>` that never made it into the JSX. Fixed by changing the `<span>` to `<button type="submit" className="form-icon">` — same look (the existing CSS already targets `button`), now actually clickable/Enter-submittable. Worth remembering when auditing any other icon-looking form control on the public site: a non-`<button>` clickable-looking element inside a `<form>` needs its own explicit `onClick` handler (or must actually be a submit button) or it silently does nothing.
- **Contact info links (fixed 2026-09-06)**: the template's contact details (email/phone/address) were plain text or dead `href="#"` links in several places — `components/others/Contact.jsx` (the `/contact` page's 3 info cards), `components/footers/Footer1.jsx`/`Footer3.jsx` (footer "Contact" column), and `components/headers/Sidebar.jsx` (the "Rejoignez moi" off-canvas panel opened from the header). All four now use real links: email → `mailto:contact@faridzaffalone.com`, phone → `tel:+33660919320` (click-to-call), address → the real Google Maps link `https://maps.app.goo.gl/YJnbFSehAHh7Kant9` (`target="_blank"`). `components/headers/MobileMenu.jsx` has the same contact block but wasn't touched — check it for the same dead-link pattern if asked to fix it too. On `/contact` specifically, the whole `.contact-info` card (icon + title + text) is the `<a>` — not just the small paragraph of text — matching a follow-up request to make the full card clickable, not just a sliver of it; `public/assets/scss/elements/_contact.scss`'s `.contact-info` rule got `text-decoration: none` (it's an anchor now, not a `div`) plus a subtle hover (`translateY` + lighter background) as a click affordance since the whole block has no visible "this is a link" cue otherwise.
- **Responsive**: the whole `/admin` section adapts down to tablet/mobile. Below 900px, `AdminSidebar.jsx` switches from a static sidebar to a fixed topbar (logo + hamburger) plus an off-canvas drawer (`.sidebar`/`.sidebarOpen`, dark `.sidebarOverlay`, closes on route change via a `pathname` effect). List tables (`Messages`, `Chat`, `Newsletter`, `Comments`, `Articles`, `Projects`, `Services`) use a `<colgroup>` with fixed px/percent widths on narrow columns (status/date/views/order/actions) combined with `table-layout: fixed` and `.card { overflow-x: auto }` so the table scrolls horizontally inside its card instead of squeezing columns unreadably — the title/name column doesn't automatically get `width: 'auto'` by convention here (the Services list explicitly balances title vs. slug at 30%/25% because an `auto` column absorbing 100% of the remaining space made it too wide; tune per-list based on what actually needs the room). `PostForm`/`ProjectForm`/`ServiceForm`'s two-column `.formLayout` (content + sticky aside) collapses to one column under 900px.
- **Footgun**: `public/assets/scss/default/_forms.scss` sets a global `button, input, select, textarea { width: 100%; }` that applies to every `<button>` in `/admin` too (it's loaded via the same root `main.scss`). Any new admin button class must explicitly set `width: auto` (see `.btn`, `.actionLink`, `.menuToggle` in `admin.module.scss`) or it can silently balloon to fill its flex container — this is what caused the mobile hamburger button's icon to render off-center instead of flush right (its invisible clickable box was stretching to fill the topbar). Buttons that *should* be full-width (e.g. login submit) don't need the property at all — they already stretch via the parent's default flex `align-items: stretch`.

## Backend (`backend/`)

### Commands

```bash
composer install
cp .env.example .env    # fill in DB + JWT_SECRET
```

No built-in dev-server script or test suite is defined (`composer.json` has no `scripts` key). `public/.htaccess` rewrites everything to `public/index.php`, which implies Apache/MAMP as the intended local server (comments in `AuthMiddleware.php` reference MAMP explicitly for the `Authorization` header workaround). To run with PHP's built-in server instead: `php -S localhost:8000 -t public`.

Since MAMP symlinks the whole `backend/` folder (not just `public/`) into htdocs, `backend/.htaccess` denies direct access to everything at that level (`.env`, `vendor/`, `database/`, `scripts/`, `composer.*`) and `public/.htaccess` re-grants access for its own subtree — this is what actually keeps DB credentials and the JWT secret from being fetchable over HTTP. If you ever add a new top-level folder under `backend/` that needs to be browser-reachable, it needs its own `Require all granted` override the same way.

**New database (fresh install, e.g. production on o2switch): run `database/schema.sql` alone.** It's a consolidated dump (generated 2026-09-06) of the cumulative result of all six migration files below, including the `services` seed data — one import, no ordering to think about.

**Existing database (upgrading local dev after pulling a new migration): apply only the new numbered file(s) you don't have yet**, in order: `database/migrations.sql`, then `database/migrations_v2.sql` (adds `users.totp_secret`/`totp_enabled`, `chat_messages`, `newsletter_subscribers`), then `database/migrations_v3.sql` (adds `categories`, `tags`, `post_tags`; renames `posts.cover_image` → `featured_image` and adds `featured_image_alt`/`excerpt`/`category_id`/`status`/`views_count`/`meta_*`; adds `projects.category`/`subtitle`/`client`/`role`/`project_date`/`tags`/`features`/`thumbnail`/`cover_image`/`live_url`/`gallery`/`meta_*`/`views_count`/`sort_order` and drops the old `projects.image_url`/`link`), then `database/migrations_v4.sql` (adds `rate_limits`, used by `Services/RateLimiter.php` to throttle login/2FA brute-force), then `database/migrations_v5.sql` (adds the `services` table — `title`/`slug`/`status` (`draft`/`published`)/`icon`/`description`/`sections` (JSON)/`image`/`image_alt`/`meta_*`/`sort_order`/`views_count`; also seeds it with the 6 real services that used to be hardcoded in `frontend/data/services.js`), then `database/migrations_v6.sql` (adds `newsletter_subscribers.viewed_at`, used to make the sidebar's "Newsletter" badge count only new/unseen subscribers instead of the total — see Admin dashboard section) against the DB named in `.env` (`DB_NAME`). These are plain numbered SQL files applied by hand — there's no migration runner/tracking table.

**Whenever a new `migrations_vN.sql` is added, update `database/schema.sql` to match** (it does not regenerate itself) — either by hand-editing it or by re-dumping structure from a fully-migrated local DB (`mysqldump --no-data`, strip the `AUTO_INCREMENT=N` values, keep the `services` seed `INSERT` at the end) and re-verifying it imports cleanly into an empty database. **This has only ever been run against local dev** — there is no production database yet, so when one is stood up, run `schema.sql` against it before deploying the corresponding backend code.

### Architecture

Minimal hand-rolled MVC, PSR-4 autoloaded under `App\` → `src/`:

- `public/index.php` — front controller: applies `CorsMiddleware`, boots `App`, includes `routes/api.php`, dispatches.
- `routes/api.php` requires `routes/public.routes.php` and `routes/admin.routes.php`, both given the `$router` instance.
- `src/Core/Router.php` — exact-match routing plus `{param}` placeholders (converted to regex capture groups on dispatch, params passed as extra args to the handler after `$request`); exact matches are tried first so pre-existing param-less routes are unaffected.
- `src/Core/{Request,Response}.php` — thin wrappers (JSON body parsing, JSON responses).
- `src/config/Database.php` — PDO singleton (mysql, reads `src/config/config.php`, which reads `.env`).
- Auth: `AuthService` (bcrypt hashing + `firebase/php-jwt` signing/verification, JWTs carry a `scope` claim: `full` or `setup_2fa`) + `TotpService` (wraps `spomky-labs/otphp` for TOTP secret generation/verification) + `AuthMiddleware::handle(string $requiredScope = 'full')` (expects `Authorization: Bearer <token>`, reads it via `getallheaders()` with `$_SERVER` fallbacks for FastCGI/MAMP; `'full'` tokens satisfy any required scope, `'setup_2fa'` tokens only satisfy routes explicitly requiring that scope or `'any'`).
- 2FA is **mandatory**: a successful password login for a user with `totp_enabled = 0` returns a `setup_2fa`-scoped token (only good for `/admin/2fa/*`), not a usable session — the frontend forces enrollment before anything else is reachable. A user with 2FA enabled must submit `{email, password, code}` together to get a `full` token.
- Controllers split `Controllers/Admin/*` (JWT-protected, behind `AuthMiddleware::handle()`) vs `Controllers/Public/*` (open, for the frontend to consume).
- Image uploads: `Services/UploadService.php` validates (jpg/jpeg/png/webp/gif, ≤4 MB, extension **and** real MIME type via `mime_content_type()`/`getimagesize()` — a renamed non-image file is rejected even if its extension is on the allow-list) and moves an uploaded file into `public/uploads/{articles|projects|projects/gallery|services}/` with a slugified+uniqid filename, returning the relative path stored in DB. `Controllers/Admin/UploadController.php` (`POST /admin/uploads`) reads `$_FILES`/`$_POST` directly — the only endpoint that bypasses `Core/Request`'s JSON body parsing, since it's multipart. Uploaded files are served as plain static files by Apache/PHP's built-in server (they exist on disk, so `public/.htaccess`'s rewrite condition `!-f` skips them) — no controller serves them. `public/uploads/.htaccess` disables script execution in that directory as defense in depth.
- `config('app.url')` (`APP_URL` in `.env`) is the backend's own browser-reachable base URL, used only to build absolute upload URLs in `UploadController`'s response (`{path, url}`). Must match whichever local server is actually running (MAMP vs PHP built-in server — see the frontend commands section above).

### Implementation status (as of 2026-09-05)

Implemented:
- Core (`Router`, `Request`, `Response`, `App`), `CorsMiddleware`, `AuthMiddleware` (with scopes)
- `AuthService`, `TotpService`, `MailService` (best-effort `mail()` wrapper, reads `MAIL_TO` from `.env` — currently `contact@faridzaffalone.com`; PHP's `mail()` has no real MTA behind it on local MAMP, so contact-form emails won't actually be delivered in dev even though the request succeeds and the message is saved to `messages` — this is expected to start working once deployed to o2switch, which has a working mail server, no code change needed at that point), `UploadService`
- `Support/Slugger.php` — plain `slugify()` helper (ASCII transliteration + dasherize), used wherever a slug or filename needs to be derived from user input
- `Database.php` singleton
- `Models/{User,Post,Project,Service,Category,Tag,Comment,Message,ChatMessage,NewsletterSubscriber}.php` — plain static-method models (`all`/`find`/`create`/`update`/`delete` as applicable), no ORM. `Post` joins `categories` and loads tags via `post_tags`; `Project` JSON-encodes/decodes `features`/`gallery` on write/read; `Service` JSON-encodes/decodes `sections` the same way and has a `published()` method (status = `published` only, used by the public controller) alongside `all()` (every status, used by the admin controller).
- `Controllers/Admin/{AuthController,SecurityController,DashboardController,PostController,ProjectController,ServiceController,CategoryController,TagController,UploadController,CommentController,MessageController,ChatMessageController,NewsletterController}.php` — full CRUD/moderation for posts, projects, services, categories, tags, image uploads, comments, contact messages, chat messages, newsletter; `DashboardController` returns unread/pending counts for the admin stat tiles (`newsletter_count` is the total active-subscriber count used by the dashboard stat tile; `newsletter_new_count`, added 2026-09-06, is the count of subscribers with `viewed_at IS NULL` and feeds the sidebar badge instead — see Admin dashboard section for how it gets cleared); `NewsletterController::markViewed` (`PUT /admin/newsletter/mark-viewed`) sets `viewed_at = NOW()` on every currently-unviewed subscriber
- `Controllers/Public/{ContactController,PostController,ProjectController,ServiceController,NewsletterController,ChatMessageController,CommentController}.php` — `ContactController::store` saves to `messages` and best-effort emails `MAIL_TO`; `Post`/`Project`/`Service` public controllers back the real public blog/portfolio/services pages (`index`/`show`, `Post::index` also filters by `?category=`/`?tag=` slug, `Post`/`Project`/`Service`'s `show` methods increment `views_count`, `Service`'s `index`/`show` only ever return `status = 'published'` rows — a draft's slug 404s on the public site even if guessed); `NewsletterController::store` backs the footer signup form (`components/footers/NewsletterForm.jsx`, used by both `Footer1` and `Footer3`) — upserts by email (re-subscribes if `unsubscribed_at` was set, otherwise silently succeeds on an already-active email, no duplicate row); `ChatMessageController::store` (2026-09-05) backs the "Message Direct" floating chat widget (`components/common/Chat.jsx`) — validates name/email/message and inserts into `chat_messages`, no email notification (the admin's unread-count badge is the moderation signal, same as contact messages); `CommentController::store` (2026-09-06) backs `components/blog/Comment.jsx`'s blog comment form — validates `post_id`/name/email/message, 404s if the post doesn't exist, inserts into `comments` with the default `status = 'pending'` (visible in `/admin/comments` for moderation, not shown on the public site until approved — there's no public listing of comments yet regardless)
- `database/migrations.sql` + `migrations_v2.sql` + `migrations_v3.sql` + `migrations_v4.sql` + `migrations_v5.sql` + `migrations_v6.sql` (full schema)
- `Services/RateLimiter.php` — DB-backed (`rate_limits` table) brute-force throttle (5 attempts / 15 min lockout by keyed string), applied to `/admin/login` (password stage and TOTP stage separately) and `/admin/2fa/{enable,disable}`

Still scaffolded, empty (0-line) — not a bug, just not written yet:
- `Models/Media.php`
- `Middlewares/ValidationMiddleware.php`
- `Core/Session.php`

All public-facing forms are now wired end to end (contact, newsletter, chat widget, blog comments) — nothing left unwired as of 2026-09-06.

`routes/public.routes.php` registers `POST /admin/login`, `POST /contact`, `POST /newsletter`, `POST /chat-messages`, `POST /comments`, and `GET /posts`, `/posts/{slug}`, `/projects`, `/projects/{slug}`, `/services`, `/services/{slug}`. `routes/admin.routes.php` registers the full CRUD surface for posts/projects/services/categories/tags/uploads/comments/messages/chat-messages/newsletter (including `PUT /admin/newsletter/mark-viewed`) plus `/admin/dashboard` and `/admin/2fa/{status,setup,enable,disable}` — all behind `AuthMiddleware::handle()` with the appropriate scope.
